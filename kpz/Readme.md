# KPZ distribution directory

This `kpz` directory is used to store any `booksCheckedOut` `kpz` files
suitable for installation in Koha.

Nothing in this directory (other than this Readme file) will be committed
to version control.

